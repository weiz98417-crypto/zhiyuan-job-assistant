# 09 — 脚本工具链

> 所属阶段：Phase 0-2 · Node.js .mjs 模块

---

## 1. 工具链总览

脚本分布在两个层级：

### 根级 `.mjs` 脚本（10 个）

这些脚本因涉及全局系统操作（扫描、测试、管道验证等），保持在项目根目录以匹配 `CLAUDE.md` 中的直接引用路径：

```
根目录/
├── scan.mjs                门户扫描器（Greenhouse/Ashby/Lever API）
├── generate-pdf.mjs        Playwright HTML → PDF
├── test-all.mjs            全量测试套件
├── merge-tracker.mjs       DEPRECATED — TSV → applications.md 桥接
├── analyze-patterns.mjs    投递模式分析
├── followup-cadence.mjs    跟进节奏计算
├── doctor.mjs              系统健康诊断
├── cv-sync-check.mjs       CV 数据同步检查
├── normalize-statuses.mjs  状态规范化（统一 states.yml 别名）
├── verify-pipeline.mjs     管道完整性验证
├── verify-desktop-layout.mjs  桌面端布局验证
├── dedup-tracker.mjs       追踪器去重
├── check-liveness.mjs      职位活跃度检查入口
└── liveness-core.mjs       活跃度检查核心逻辑（共享）
```

### `scripts/` 子目录脚本（6 个）

这些脚本是 Agent 运行时的核心工具，作为独立模块放在 `scripts/` 下：

```
scripts/
├── db-write.mjs           Agent → SQLite 写入桥接
├── scan-risks.mjs         风险智能检测引擎（3 层）
├── validate-output.mjs    LLM 输出校验（score/date/status/path）
├── check-onboarding.mjs   数据完整性硬阻断
├── next-report-num.mjs    原子报告编号分配
├── simulate-risk-report.mjs  风险报告模拟（开发/测试用）
```

**总计：20 个脚本**

---

## 2. 核心脚本详解

### 2.1 db-write.mjs

**位置**：`scripts/db-write.mjs`

**用途**：Claude Agent 写入 SQLite 的桥接层。封装 `better-sqlite3`，提供三个 action。

```bash
# 写入/更新应用记录
node scripts/db-write.mjs --action upsertApp --data '{"num":42,...}'

# 写入/更新报告元数据
node scripts/db-write.mjs --action upsertReport --data '{"report_num":42,...}'

# 写入 JD
node scripts/db-write.mjs --action insertJD --data '{...}'
```

**安全特性**：
- 输入净化：控制字符过滤（`[\x00-\x08\x0b\x0c\x0e-\x1f]`）
- score 格式校验：必须 1.0-5.0
- date 格式校验：必须 YYYY-MM-DD
- SQLITE_BUSY 指数退避重试（100ms→400ms→2s, 3 次）
- 错误日志自动写入 `data/db-write-errors.log`

**注意**：Next.js API 端的主要写入路径是 `server-db.ts`（直接 better-sqlite3），`db-write.mjs` 仅用于 Claude Agent 端（无法直接 import TypeScript 模块）。TSV fallback 已降级为 legacy 容错手段，不再推荐。

---

### 2.2 scan-risks.mjs

**位置**：`scripts/scan-risks.mjs`

**用途**：风险智能检测引擎 — 3 层递进检测。

```bash
# 文本输入
node scripts/scan-risks.mjs --jd-text "JD正文..."

# 文件输入
node scripts/scan-risks.mjs --jd-file ./jds/001-bytedance.md
```

**三层检测架构**：

| 层 | 方法 | 覆盖信号 | 精确度 |
|---|------|---------|--------|
| **Layer 1** — 字面正则 | `risk-intel-triggers.yml` 正则匹配 | 35 条硬编码正则（黑话字面检测） | 高精确、低召回 |
| **Layer 2** — 语义模式 | 结构化模式匹配（上下文判定） | 模糊信号（如"弹性工作"需结合上下文判断是否为加班信号） | 中等 |
| **Layer 3** — LLM 推理 | `/api/agent/scan-risks` → LLM 语义分析 | 隐含风险（需要推理的信号） | 低精确、高召回 |

**Layer 1 详细流程**：
1. 读取 `risk-intel-triggers.yml`（35 条正则 + 语义模式）
2. 对 JD 文本逐个执行 `new RegExp(pattern, 'gi')`
3. 去重：同 signal+severity 保留最长 excerpt
4. 输出 JSON `[{signal, excerpt, severity}, ...]`
5. 无匹配 → `[]`
6. triggers.yml 缺失 → `[]`（不崩溃）
7. JD < 50 字符 → `[]`（空输入 guard）

**三层联合使用**：Layer 1（脚本）提供快速确定性检测，Layer 2-3 由服务端 `/api/agent/scan-risks` route 内部调用 LLM 补充。

---

### 2.3 validate-output.mjs

**位置**：`scripts/validate-output.mjs`

**用途**：LLM 输出在持久化前的最后一道校验。

```bash
node scripts/validate-output.mjs --data '{
  "overall_score": 4.2,
  "date": "2026-05-09",
  "status": "Applied",
  "report_path": "reports/042-bytedance-2026-05-09.md"
}'
# Exit 0 = 通过, Exit 1 = 失败
```

**校验规则**：

| 字段 | 规则 | 失败动作 |
|------|------|---------|
| `overall_score` | 1.0 ≤ score ≤ 5.0, 数字类型 | 报告拒绝, 重新生成 |
| `date` | YYYY-MM-DD, 合法日期 | 报告拒绝, 重新生成 |
| `status` | 在 states.yml 中, 或已知别名 | 自动修正为规范值 |
| `report_path` | `reports/NNN-slug-YYYY-MM-DD.md` | 报告拒绝 |

**降级策略**：
- `states.yml` 缺失 → 跳过状态校验（不崩溃），使用硬编码 fallback
- 别名自动映射（如 "已投递" → "Applied"）

---

### 2.4 check-onboarding.mjs

**位置**：`scripts/check-onboarding.mjs`

**用途**：入职数据完整性检查。CV/profile 为空时硬阻断评估。

```bash
node scripts/check-onboarding.mjs
# Exit 0 = 通过, Exit 1 = 阻断
```

**检查项**：
1. `cv.md` 存在且 >50 有效字符，不含占位符标记
2. `config/profile.yml` 存在且姓名不是模板示例
3. `modes/_profile.md` 存在

**多语言占位符检测**：
- 中文：请在此处填写、在此输入、填写你的
- 英文：TODO: fill、Your experience here、Paste your CV
- 日文：ここに入力
- 韩文：여기에 입력

**示例姓名检测**：
- 张三、李四、王五、Your Name、姓名、Max Mustermann、山田太郎

---

### 2.5 next-report-num.mjs

**位置**：`scripts/next-report-num.mjs`

**用途**：原子报告编号分配，防止并行 Agent 覆盖文件。

```bash
node scripts/next-report-num.mjs
# 输出: 42
```

**实现**：`mkdirSync` 文件系统原子锁
1. 扫描 `reports/` 找到最大编号 N
2. 尝试 `mkdirSync(reports/.locks/NNN)`
3. 成功 → 返回 NNN（原子获取）
4. 失败（EEXIST）→ NNN++ 重试
5. 大于 1 小时的锁目录自动清理

---

### 2.6 simulate-risk-report.mjs

**位置**：`scripts/simulate-risk-report.mjs`

**用途**：风险检测 demo——展示评估报告中实际出现的格式。

```bash
node scripts/simulate-risk-report.mjs --jd-text "可疑JD..."
```

输出完整的 `🛡️ 风险提示` 表格 + 风险等级 + 评分影响 + 建议。

---

### 2.7 scan.mjs

**位置**：根目录 `scan.mjs`

**用途**：零 token 门户扫描器。直接调用 ATS API，不使用 LLM。

```bash
node scan.mjs                  # 扫描所有 api 标记的公司
node scan.mjs --dry-run        # 预览模式
node scan.mjs --company Cohere # 单公司扫描
```

**支持的 ATS**：
- ✅ Greenhouse (boards.greenhouse.io)
- ✅ Ashby (jobs.ashbyhq.com)
- ✅ Lever (jobs.lever.co)

**不支持（需 Playwright）**：
- ❌ Boss直聘、拉勾、猎聘、51job、智联招聘
- ❌ LinkedIn、Indeed

`portals.yml` 的 `scan_method` 字段控制：`api` → 扫描；`playwright` → 跳过

---

### 2.8 generate-pdf.mjs

**位置**：根目录 `generate-pdf.mjs`

**用途**：Playwright HTML → PDF 生成。

流程：
1. 读取 `/tmp/cv-{name}-{company}.html`
2. Playwright 打开 HTML
3. 生成 PDF（A4, 边距 0.5in, 打印背景）
4. 输出到 `output/cv-{name}-{company}-{date}.pdf`

---

### 2.9 check-liveness.mjs + liveness-core.mjs

**位置**：根目录

**用途**：职位发布活跃度检测。`liveness-core.mjs` 包含共享的活跃度判定逻辑（内容检测 + HTTP 状态码分析），`check-liveness.mjs` 为命令行入口，支持单 URL 或批量检测。

```bash
node check-liveness.mjs --url "https://..."
node check-liveness.mjs --batch ./data/pipeline.md
```

---

### 2.10 doctor.mjs

**位置**：根目录 `doctor.mjs`

**用途**：系统健康诊断。检查 SQLite 可访问性、文件完整性、模式一致性、数据契约合规性。一键诊断全系统状态。

```bash
node doctor.mjs
# 输出：逐项检查结果 + 通过/失败/警告统计
```

---

### 2.11 normalize-statuses.mjs

**位置**：根目录 `normalize-statuses.mjs`

**用途**：批量将 `applications` 表中使用别名（如 "已投递"、"淘汰"）的 status 字段规范化为 `templates/states.yml` 定义的 canonical values。

```bash
node normalize-statuses.mjs
# 输出：修正 N 条记录
```

---

### 2.12 verify-pipeline.mjs

**位置**：根目录 `verify-pipeline.mjs`

**用途**：管道完整性验证。检查 `data/pipeline.md` 中的 URL 与 `applications` 表中的记录是否一致，发现孤立的 pipeline 条目或缺失的报告引用。

```bash
node verify-pipeline.mjs
# Exit 0 = 一致, Exit 1 = 发现不一致
```

---

### 2.13 其他根级脚本

| 脚本 | 用途 |
|------|------|
| `analyze-patterns.mjs` | 投递模式分析（JSON 输出），识别回复率、转化率趋势 |
| `followup-cadence.mjs` | 跟进节奏计算器，基于投递日期推荐 follow-up 时机 |
| `merge-tracker.mjs` | **DEPRECATED** — TSV → applications.md 的批量导入桥接，已被 db-write.mjs 替代 |
| `cv-sync-check.mjs` | CV 数据同步检查：`cv.md` ↔ `cv_data` 表一致性 |
| `verify-desktop-layout.mjs` | 桌面端布局验证，检查响应式断点和页面结构 |
| `dedup-tracker.mjs` | 追踪器去重，检测并报告 applications 表中的重复记录 |

---

## 3. test-all.mjs

**位置**：根目录 `test-all.mjs`

**用途**：全量测试套件。每次 push 前运行。

```
📊 Results: 54 passed, 0 failed, 0 warnings
```

**覆盖（10 项）**：
1. 脚本语法检查（所有 .mjs 文件可解析）
2. 脚本执行检查（关键脚本可运行）
3. 活跃度分类（check-liveness.mjs）
4. 系统文件存在性（24 个文件）
5. 数据契约（9 项）
6. 个人数据不出现在系统文件中（私有数据检查）
7. Mode 文件完整性（全部 mode 存在）
8. Mode 交叉引用（_shared.md 引用 _profile.md）
9. CLAUDE.md 完整性（核心 section 存在）
10. VERSION 文件（有效 semver）

**Phase 2 变化**：Go dashboard 删除后，dashboard 相关测试项已移除。测试覆盖数量从 59 调整为 54。
