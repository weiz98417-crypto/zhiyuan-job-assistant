# 治理拒绝动作而非默认终止 Run

我们决定把明确的治理决策作用于当前 Tool Attempt：拒绝后将结构化原因交回 Agent Run，使其可以选择合规路径；只有用户取消、明确的安全停止、不可恢复前置条件或恢复预算耗尽才结束 Run。治理基础设施自身异常可以对只读动作降级，但高风险写入必须等待治理恢复或用户处理；checkpoint、事务、幂等和读回仍然 fail-closed。这个区分保留安全约束，同时避免治理与监控基础设施劫持整个任务生命周期。
